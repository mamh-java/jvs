version: '3'
services:
  jvs-ui:
    build:
      context: .
    restart: always
    container_name: jvs-ui
    image: jvs-ui
    networks:
      - jvs_jvs
    external_links:
      - gateway
    ports:
      - 8080:80

networks:
  jvs_jvs:
    external: true
